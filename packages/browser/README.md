# @fabricjs/browser

Browser entrypoint for Fabric.js.

This package re-exports `@fabricjs/core` for browser and SSR-oriented installs that should not pull node-specific `canvas` and `jsdom` dependencies.
